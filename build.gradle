plugins {
    id 'base'
}

tasks.register('backendBuild', GradleBuild) {
    dir = file('apps/backend')
    tasks = ['build']
}

tasks.register('backendTest', GradleBuild) {
    dir = file('apps/backend')
    tasks = ['clean', 'test']
}

tasks.named('build') {
    dependsOn('backendBuild')
}
